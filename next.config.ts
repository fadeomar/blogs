import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["loremflickr.com", "lh3.googleusercontent.com", "picsum.photos"],
  },
};

export default nextConfig;
